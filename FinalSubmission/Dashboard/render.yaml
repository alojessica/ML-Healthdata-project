services:
  - type: web
    name: mental-health-dashboard
    env: python
    rootDir: FinalSubmission/Dashboard
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn app:server --timeout 300 --workers 2
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.9